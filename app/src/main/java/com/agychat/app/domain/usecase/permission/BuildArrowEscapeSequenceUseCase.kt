package com.agychat.app.domain.usecase.permission

import javax.inject.Inject

/**
 * Use case to build arrow escape sequence.
 */
class BuildArrowEscapeSequenceUseCase @Inject constructor() {
    operator fun invoke(delta: Int): ByteArray {
        // Implementation omitted for skeleton
        return ByteArray(0)
    }
}
